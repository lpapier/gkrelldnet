dnetw:
- use a shm to communicate to monitoring apps.
- improve % reading accuracy.
- support other Unix system (FreeBSD, ...)

gtkrellm plugin:
- nicer support for SMP and multi-processor system.
- scrolling decal if text is larger than gkrellm width.

